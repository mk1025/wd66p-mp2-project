// BUTTONS
export const EXIT_BUTTON = document.getElementById(
  "Homepage_Button",
) as HTMLButtonElement;
export const LOGIN_BUTTON = document.getElementById(
  "login_button",
) as HTMLButtonElement;

// INPUTS

export const CREDENTIAL_INPUT = document.getElementById(
  "email_username",
) as HTMLInputElement;
export const PASSWORD_INPUT = document.getElementById(
  "password",
) as HTMLInputElement;

// ALERTS

export const ALERT_DIV = document.getElementById("alert") as HTMLDivElement;

// SPINNER

export const SPINNER_DIV = document.getElementById("spinner") as HTMLDivElement;
